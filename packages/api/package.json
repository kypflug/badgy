{
  "name": "@badgy/api",
  "version": "0.1.0",
  "private": true,
  "description": "Badgy BFF — token-mediating backend (MSAL Node confidential client) for durable sign-in.",
  "engines": {
    "node": "20"
  },
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "npm run build && node --test dist/src/test/*.test.js"
  },
  "dependencies": {
    "@azure/data-tables": "^13",
    "@azure/functions": "^4",
    "@azure/msal-node": "^2"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.7.0"
  }
}
