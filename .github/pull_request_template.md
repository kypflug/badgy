## What and why

<!-- What changes, and what problem it solves. Link an issue if there is one. -->

## Checks

- [ ] `npm run gates` passes locally
- [ ] `npm --prefix packages/api test` passes, if the API changed
- [ ] Documentation updated, if behaviour changed — `README.md`, `docs/SETUP.md`, and both
      `CLAUDE.md` and `.github/copilot-instructions.md` (those two are kept in sync)

<!--
Adding or correcting an employer's RTO policy, or a holiday set?

Use the policy template instead — add `?template=policy.md` to the URL when opening the PR,
or copy the checklist from `.github/PULL_REQUEST_TEMPLATE/policy.md`. Start with `data/README.md`;
it covers the schema, the sourcing rules, and how to record assumptions.
-->
